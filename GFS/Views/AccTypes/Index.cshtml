@model IEnumerable<GFS.Models.AccType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.accNType)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.accNType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.typeId }) |
                @Html.ActionLink("Details", "Details", new { id = item.typeId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.typeId })
            </td>
        </tr>
    }

</table>
