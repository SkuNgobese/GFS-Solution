@model IEnumerable<GFS.Models.Bank>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bankN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.code)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.bankN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.code)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.bankId }) |
                @Html.ActionLink("Details", "Details", new { id = item.bankId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.bankId })
            </td>
        </tr>
    }

</table>
