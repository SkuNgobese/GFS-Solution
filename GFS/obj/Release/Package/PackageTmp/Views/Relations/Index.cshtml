@model IEnumerable<GFS.Models.Relation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.relationsh)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.relationsh)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.relationNo }) |
                @Html.ActionLink("Details", "Details", new { id = item.relationNo }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.relationNo })
            </td>
        </tr>
    }

</table>
